{% extends 'base.html.twig' %}

{% block title %}{{ title }} - {{ parent() }}{% endblock %}

{% block header %}<h1 class="{{ theme('headerTitle') }} rounded-lg">{{ title }}</h1>{% endblock %}

{% block body %}
    <article class="{{ theme('container') }} p-6">
        <div class="{{ theme('content') }}">
            {{ content|raw }}
        </div>
    </article>
{% endblock %}
